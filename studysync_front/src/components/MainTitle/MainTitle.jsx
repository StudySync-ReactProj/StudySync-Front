import { Title } from "./MainTitle.style";

const MainTitle = ({ title }) => {
    return (
        <div id="Title">
            <Title>{title}</Title>
        </div>
    );
};

export default MainTitle;